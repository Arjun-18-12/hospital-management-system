[tool.pyright]
venvPath = "."
venv = "backend/venv"
include = ["backend", "view_users.py", "verify_backend.py"]
extraPaths = ["backend/venv/Lib/site-packages", "backend"]

[tool.isort]
profile = "black"
